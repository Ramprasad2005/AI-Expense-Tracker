// Centralized configuration for the API endpoints.
// During development, point this to your localhost server.
// During deployment, replace this with your hosted backend URL (e.g. Render, Railway, etc.)
export const API_BASE_URL = 'https://ai-expense-tracker-zzhn.vercel.app/api';
